#!/usr/bin/env bash
set -u

killall -q polybar 2>/dev/null || true

while pgrep -x polybar >/dev/null 2>&1; do
    sleep 0.1
done

polybar main >>"${XDG_STATE_HOME:-$HOME/.local/state}/polybar.log" 2>&1 &
